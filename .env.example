# Copy to .env and chmod 600 it. .env is gitignored — never commit a key.
# Generate the key in Atera under Admin > API. It is read access to your whole RMM.
ATERA_API_KEY=

# Only used when pulling the published image rather than building locally.
# Pin a version instead of riding latest.
IMAGE_TAG=latest
